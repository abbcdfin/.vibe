---
description: Initialize the current directory as a vibe project — single-repo (default), or hub/spoke for a multi-repository workspace.
argument-hint: "[single|hub|spoke]"
---

You are initializing the current directory as a **vibe** project. The requested
mode is: `$1` (default to `single` if empty).

Execute the full procedure in `skills/vibe-init/reference.md` for that mode — it is
the single source of truth for the scaffolding steps (shared with the cross-tool
`vibe-init` skill). Read it and follow it exactly, including its directory-safety
check and the "after scaffolding" workflow.
